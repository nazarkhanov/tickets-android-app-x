<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Tickets" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue_200</item>
        <item name="colorPrimaryVariant">@color/blue_700</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/white</item>

        <item name="windowBackground">@color/black_700</item>
        <item name="windowContrast">@color/white</item>

        <item name="android:windowBackground">@color/black_700</item>

        <item name="actionBarStyle">@style/ThemeActionBar</item>
    </style>

    <style name="ThemeActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="background">@color/black_500</item>
    </style>
</resources>